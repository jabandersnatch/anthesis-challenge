# ==============================================================================
# Environmental Emissions API - Environment Variables
# ==============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control!

# ==============================================================================
# Django Settings
# ==============================================================================

# Secret key for Django (generate a new one for production!)
# You can generate a new secret key using: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-wiuub6mgsldym5!9w030+om_2v4&l0wpsd@z+#!2nzv$kqbo&f

# Debug mode (set to False in production!)
DEBUG=True

# Allowed hosts (comma-separated, e.g., localhost,127.0.0.1,yourdomain.com)
ALLOWED_HOSTS=localhost,127.0.0.1


# ==============================================================================
# Database Configuration (PostgreSQL)
# ==============================================================================

# Database engine (use django.db.backends.postgresql for PostgreSQL)
DB_ENGINE=django.db.backends.postgresql

# Database name
DB_NAME=environmental_emissions

# Database user
DB_USER=postgres

# Database password
DB_PASSWORD=your_password_here

# Database host (use 'localhost' for local development)
DB_HOST=localhost

# Database port (default PostgreSQL port is 5432)
DB_PORT=5432


# ==============================================================================
# CORS Configuration (for Angular Frontend)
# ==============================================================================

# Allowed origins for CORS (comma-separated)
# For local Angular development, typically: http://localhost:4200
# For production, use your actual frontend domain(s)
CORS_ALLOWED_ORIGINS=http://localhost:4200


# ==============================================================================
# Optional Settings
# ==============================================================================

# Add any additional environment variables below as needed
